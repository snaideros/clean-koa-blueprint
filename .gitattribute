# .gitattributes - Configuration for git archive and line endings

# 1. Exclude template documentation and legal files from the final archive
README.md export-ignore
LICENSE export-ignore

# 2. Exclude template-specific configuration
.gitattributes export-ignore

# #. Ensure scripts use LF (Line Feed) to prevent cross-platform bugs (Windows/Linux)
*.sh text eol=lf
*.js text eol=lf
*.ts text eol=lf

# $. Treat specific files as binary
*.png binary
*.jpg binary